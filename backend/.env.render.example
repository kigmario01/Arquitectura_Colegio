NODE_ENV=production
PORT=${PORT}
DATABASE_URL=${tu_url_de_bd_produccion}
SECRET_KEY=${tu_clave_secreta}
API_KEY=${tu_api_key}
# Agrega aquí otras variables específicas de tu proyecto

# Nota: Este backend usa SQLite por defecto.
# Si mantienes SQLite en Render, usa en su lugar:
# DB_PATH=/var/data/plataforma_estudiantil.sqlite
# Y en lugar de SECRET_KEY/JWT_SECRET puedes definir:
# JWT_SECRET=${tu_clave_secreta}
# También puedes restringir CORS a tu dominio de Vercel:
# CORS_ORIGIN=https://tu-app.vercel.app